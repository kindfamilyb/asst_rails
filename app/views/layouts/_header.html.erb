<%# 데스크톱 상단 네비게이션 바 %>
<header class="hidden md:block fixed top-0 left-0 w-full bg-white border-b border-gray-200 z-50">
  <div class="container mx-auto px-4">
    <div class="flex items-center justify-between h-16">
      <%# 로고 영역 %>
      <%= link_to root_path, class: "flex items-center" do %>
        <span class="text-xl font-bold text-gray-800">ASST</span>
      <% end %>

      <%# 네비게이션 메뉴 %>
      <nav class="flex items-center space-x-8">
        <%= link_to "홈", root_path, class: "text-gray-600 hover:text-gray-900" %>
        <%= link_to "업비트", upbit_accounts_path, class: "text-gray-600 hover:text-gray-900" %>
        <%= link_to "패키지마켓", packages_index_path, class: "text-gray-600 hover:text-gray-900" %>
        <%= link_to "세팅", settings_index_path, class: "text-gray-600 hover:text-gray-900" %>
        <% if current_user %>
          <div class="flex items-center justify-center">
              <%= image_tag(current_user.avatar_url, style: "width: 3rem; height: 3rem; border-radius: 50%;") %>
          </div>
        <% else %>
          <%= link_to "로그인", new_user_session_path, class: "text-gray-600 hover:text-gray-900" %>
        <% end %>
      </nav>
    </div>
  </div>
</header> 