<h1><%= @package.title %></h1>
<div>
  <p><strong>설명:</strong> <%= @package.description %></p>
  <p><strong>다운로드 수:</strong> <%= @package.download_count %></p>
</div>
<%= link_to "뒤로 가기", packages_path %> 